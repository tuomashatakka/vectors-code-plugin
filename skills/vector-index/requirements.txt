# Global project-partitioned local RAG — runtime deps (all local, no API keys)
zvec>=0.5.0                       # in-process vector store (HNSW + rerank)
sentence-transformers>=2.2.0     # local embedding + cross-encoder rerank models
# ^ pulls in PyTorch (~400 MB). macOS gets a CPU/MPS wheel automatically; on Linux
#   setup.sh pre-installs the CPU-only torch wheel so you skip the multi-GB CUDA build.
numpy>=1.24                      # PCA / knn for the 3D viewer
mcp>=1.2.0                       # FastMCP stdio server (the project-aware RAG tools)

# Unified Knowledge Database daemon (daemon/ukdb_daemon.py) — installed by
# daemon/install.sh into this venv; uncomment to include in `bash setup.sh`.
# psycopg[binary]>=3.1           # Postgres client for the background sync daemon

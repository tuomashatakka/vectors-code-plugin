# python venv (recreated via setup.sh)
skills/vector-index/.venv/
**/__pycache__/
*.pyc

# local vector stores / model caches should never be committed
*.zvec/
.cache/

# stale packaged skill bundle (regenerated by the user's tooling)
*.skill

# os cruft
.DS_Store
node_modules/
.logs
*.log

# local daemon config (contains UKDB_DSN) — never commit
skills/vector-index/daemon/ukdb-daemon.env

# local env (DSN etc.) — never commit
.env
.env.local
